export default replace;
/**
 * @param {Record<string, unknown>} options
 * @returns {import('@rollup/browser').Plugin}
 */
declare function replace(options: Record<string, unknown>): import('@rollup/browser').Plugin;
